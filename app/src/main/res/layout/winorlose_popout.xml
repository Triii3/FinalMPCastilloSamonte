<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#9BFF6B">

    <TextView
        android:id="@+id/txtMatchStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:fontFamily="sans-serif-black"
        android:text="You win!"
        android:layout_marginVertical="30dp"
        android:textSize="64sp" />
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:layout_below="@id/txtMatchStatus">

        <Button android:id="@+id/btnRetry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/green_btn_round_corners"
            android:text="Retry"
            android:textAllCaps="false"
            android:fontFamily="sans-serif-black"
            android:textSize="16sp"
            android:layout_marginHorizontal="10dp"
             />
        <Button android:id="@+id/btnExit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/red_btn_round_corners"
            android:text="Exit"
            android:textAllCaps="false"
            android:fontFamily="sans-serif-black"
            android:textSize="16sp"
            android:layout_marginHorizontal="10dp"
            />
    </LinearLayout>
</RelativeLayout>